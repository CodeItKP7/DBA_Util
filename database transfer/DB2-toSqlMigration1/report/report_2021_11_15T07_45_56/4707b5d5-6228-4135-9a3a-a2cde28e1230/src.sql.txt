CREATE VIEW LDATATST.SCRAFLIP_VIEW (
    SCRA_PK_ID,
    SCRA_CREATED_TIMESTAMP,
    SCRA_CREATED_BY,
    SCRA_UPDATED_TIMESTAMP,
    SCRA_UPDATED_BY,
    SCRA_PROCESS,
    SCRA_STATUS,
    SCRA_LOAN_NBR,
    SCRA_FLIP_DATE,
    SCRA_FLAG,
    SCRA_LATE_CHG_FLAG
) AS
SELECT
 ID,
 CREATEDTS,
 CREATEDBY,
 UPDATEDTS,
 UPDATEDBY,
 PROCESS,
 STATUS,
 SCLOANNO,
 SCFLDATE,
 SCSCFLAG,
 SCLCFLAG
FROM LDATATST.SCRAFLIP